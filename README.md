# Lake3Tools-CSV-Compare
Compare very large CSV files locally, even with missing or mismatched headers. Handles millions of rows with a simple interface and includes a fully featured 3-day free trial.
